name: movies_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  dropdown_search_edited:
    path: ./custom_packages/dropdown_search_edited
  flutter_toggle_tab_edited:
    path: ./custom_packages/flutter_toggle_tab_edited

  cupertino_icons: any
  google_fonts: any
  cached_network_image: any
  flutter_dotenv: any
  provider: any
  http: any
  firebase_core: any
  cloud_firestore: any
  connectivity_plus: ^6.1.0
  oktoast: ^3.4.0
  overlay_support: ^2.1.0
  youtube_player_flutter: ^9.1.2
  another_flutter_splash_screen: any
  sqflite: any
  bloc: ^9.0.1
  flutter_bloc: ^9.1.1
  dartz: any
  get_it: any
  equatable: ^2.0.7
  easy_localization: any
  dio: any
  shared_preferences: ^2.5.2
  path: any
  sizer: ^2.0.15
  win32: ^5.11.0
  flutter_inappwebview: ^6.1.5
  path_provider: ^2.1.5
  secure_application: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: any

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/splash/
    - assets/images/browse/
    - .env
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
